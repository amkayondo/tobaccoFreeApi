const path = require("path");
require("dotenv").config();

const serverDist = process.env.SERVER_DIST;

module.exports = {
    "config": path.resolve(serverDist, "config/config.js"),
    "models-path": path.resolve(serverDist, "models"),
    "seeders-path": path.resolve(serverDist, "seeders"),
    "migrations-path": path.resolve(serverDist, "migrations"),
}